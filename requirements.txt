# ── Core: camera + vision + emotion pipeline (needed everywhere) ──────────────
numpy>=1.24
opencv-python>=4.8
mediapipe>=0.10.9
joblib>=1.3
ultralytics>=8.3
scikit-learn>=1.4
psutil>=5.9
matplotlib>=3.7          # confusion-matrix PNG for affect-pi-eval-emotion

# ── Desktop robot-eyes simulator: affect-pi-eyes --sim (skip on the Pi) ───────
pygame>=2.5 ; platform_machine not in "aarch64 armv7l armv6l"

# ── Raspberry Pi 5 hardware only: Pi Camera 2 + ST7789 SPI panels ─────────────
picamera2>=0.3.18 ; platform_machine in "aarch64 armv7l armv6l"
spidev>=3.6 ; platform_machine in "aarch64 armv7l armv6l"
gpiozero>=2.0 ; platform_machine in "aarch64 armv7l armv6l"
lgpio>=0.2 ; platform_machine in "aarch64 armv7l armv6l"

# ── Tests (also available via: pip install -e .[dev]) ─────────────────────────
pytest>=8.0
