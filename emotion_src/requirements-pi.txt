# Runtime deps for the Pi deploy (no training/YOLO/matplotlib needed).
# picamera2 + lgpio are best installed via apt (see setup_pi.sh); listed here as
# a pip fallback. Install with:  pip install -r requirements-pi.txt
numpy>=1.24
opencv-python>=4.8
mediapipe>=0.10.9
joblib>=1.3
scikit-learn>=1.4
psutil>=5.9
spidev>=3.6
gpiozero>=2.0
lgpio>=0.2
# picamera2>=0.3.18   # prefer: sudo apt install -y python3-picamera2
